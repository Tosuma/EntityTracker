prefix_rule(
    pattern = ["dotnet", "build"],
    decision = "allow",
)

prefix_rule(
    pattern = ["dotnet", "test"],
    decision = "allow",
)

prefix_rule(
    pattern = ["dotnet", "restore"],
    decision = "allow",
)