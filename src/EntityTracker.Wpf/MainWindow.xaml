<Window x:Class="EntityTracker.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:domain="clr-namespace:EntityTracker.Domain;assembly=EntityTracker.Domain"
        xmlns:viewModels="clr-namespace:EntityTracker.Wpf.ViewModels"
        xmlns:views="clr-namespace:EntityTracker.Wpf.Views"
        Title="EntityTracker"
        Icon="Assets/v2/EntityTracker.ico"
        Width="1240"
        Height="780"
        MinWidth="900"
        MinHeight="600"
        Background="{DynamicResource Brush.Surface.Page}"
        Foreground="{DynamicResource Brush.Text.Primary}"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="ShellNavigationButtonStyle"
               TargetType="ToggleButton"
               BasedOn="{StaticResource {x:Type ToggleButton}}">
            <Setter Property="Margin" Value="0,2" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.OnDark}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="AutomationProperties.HelpText"
                    Value="Navigate within EntityTracker" />
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Brush.Brand.Green80}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Brand.Green60}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="IsHitTestVisible" Value="True" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Catalog.IsOpen}" Value="True">
                            <Setter Property="IsHitTestVisible" Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="236" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Padding="18,20"
                    Background="{DynamicResource Brush.Surface.Primary}">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top">
                        <TextBlock FontSize="24"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource Brush.Text.OnDark}"
                                   Text="EntityTracker" />
                        <TextBlock Margin="0,4,0,22"
                                   Foreground="{DynamicResource Brush.Text.OnDarkMuted}"
                                   Text="Implementation portfolio"
                                   TextWrapping="Wrap" />

                        <ToggleButton Command="{Binding NavigateCommand}"
                                CommandParameter="{x:Static viewModels:ShellDestination.Portfolio}"
                                IsChecked="{Binding IsPortfolio, Mode=OneWay}"
                                Content="Portfolio"
                                Style="{StaticResource ShellNavigationButtonStyle}" />
                        <ToggleButton Command="{Binding NavigateCommand}"
                                CommandParameter="{x:Static viewModels:ShellDestination.ProjectDashboard}"
                                IsChecked="{Binding IsProjectDashboard, Mode=OneWay}"
                                Content="Project dashboard"
                                ToolTip="Select a Project to open its dashboard."
                                Style="{StaticResource ShellNavigationButtonStyle}" />

                        <TextBlock Margin="8,18,0,4"
                                   Foreground="{DynamicResource Brush.Text.OnDarkMuted}"
                                   FontWeight="SemiBold"
                                   Text="TRACKER" />
                        <ToggleButton Command="{Binding NavigateCommand}"
                                CommandParameter="{x:Static viewModels:ShellDestination.Overview}"
                                IsChecked="{Binding IsOverview, Mode=OneWay}"
                                Content="Overview"
                                ToolTip="Select a Tracker to open Overview."
                                Style="{StaticResource ShellNavigationButtonStyle}" />
                        <ToggleButton Command="{Binding NavigateCommand}"
                                CommandParameter="{x:Static viewModels:ShellDestination.Archived}"
                                IsChecked="{Binding IsArchived, Mode=OneWay}"
                                Content="Archived"
                                ToolTip="Select a Tracker to view archived entities."
                                Style="{StaticResource ShellNavigationButtonStyle}" />
                        <ToggleButton Command="{Binding NavigateCommand}"
                                CommandParameter="{x:Static viewModels:ShellDestination.Reports}"
                                IsChecked="{Binding IsReports, Mode=OneWay}"
                                Content="Reports"
                                ToolTip="Select a Tracker to open Reports."
                                Style="{StaticResource ShellNavigationButtonStyle}" />

                        <TextBlock Margin="8,18,0,4"
                                   Foreground="{DynamicResource Brush.Text.OnDarkMuted}"
                                   FontWeight="SemiBold"
                                   Text="MANAGE" />
                        <ToggleButton Command="{Binding NavigateCommand}"
                                CommandParameter="{x:Static viewModels:ShellDestination.SchemaSynchronization}"
                                IsChecked="{Binding IsSchemaSynchronization, Mode=OneWay}"
                                Content="Schema synchronization"
                                ToolTip="Select a Tracker to synchronize its schema."
                                Style="{StaticResource ShellNavigationButtonStyle}" />
                        <ToggleButton Command="{Binding NavigateCommand}"
                                CommandParameter="{x:Static viewModels:ShellDestination.AddEntity}"
                                IsChecked="{Binding IsAddEntity, Mode=OneWay}"
                                Content="Add entity"
                                ToolTip="Select a Tracker before adding an entity."
                                Style="{StaticResource ShellNavigationButtonStyle}" />
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Bottom">
                        <TextBlock Margin="8,0,0,4"
                                   Foreground="{DynamicResource Brush.Text.OnDarkMuted}"
                                   FontWeight="SemiBold"
                                   Text="UTILITIES" />
                        <ToggleButton Command="{Binding NavigateCommand}"
                                CommandParameter="{x:Static viewModels:ShellDestination.HelpSql}"
                                IsChecked="{Binding IsHelpSql, Mode=OneWay}"
                                Content="Help &amp; SQL"
                                Style="{StaticResource ShellNavigationButtonStyle}" />
                        <ToggleButton Command="{Binding NavigateCommand}"
                                CommandParameter="{x:Static viewModels:ShellDestination.Settings}"
                                IsChecked="{Binding IsSettings, Mode=OneWay}"
                                Content="Settings"
                                Style="{StaticResource ShellNavigationButtonStyle}" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Padding="20,12"
                        Background="{DynamicResource Brush.Surface.Card}"
                        BorderBrush="{DynamicResource Brush.Border.Default}"
                        BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="280" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="280" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock FontSize="12"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="CURRENT CONTEXT" />
                            <TextBlock FontSize="18"
                                       FontWeight="SemiBold"
                                       Text="{Binding ContextSummary}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <Label x:Name="ProjectSelectorLabel" Content="Project" />
                            <ComboBox x:Name="ProjectSelector"
                                      AutomationProperties.LabeledBy="{Binding ElementName=ProjectSelectorLabel}"
                                      DisplayMemberPath="Name"
                                      ItemsSource="{Binding Projects}"
                                      SelectedItem="{Binding SelectedProject, Mode=OneWay}"
                                      SelectionChanged="OnProjectSelectionChanged" />
                        </StackPanel>
                        <StackPanel Grid.Column="3">
                            <Label x:Name="TrackerSelectorLabel" Content="Tracker" />
                            <ComboBox x:Name="TrackerSelector"
                                      AutomationProperties.LabeledBy="{Binding ElementName=TrackerSelectorLabel}"
                                      DisplayMemberPath="Name"
                                      IsEnabled="{Binding HasProject}"
                                      ItemsSource="{Binding Trackers}"
                                      SelectedItem="{Binding SelectedTracker, Mode=OneWay}"
                                      SelectionChanged="OnTrackerSelectionChanged" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="1"
                        Margin="18,12,18,0"
                        Padding="12"
                        Background="{DynamicResource Brush.Surface.Card}"
                        BorderBrush="{DynamicResource Brush.Border.Default}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Visibility="{Binding ShowDefaultNamePrompt, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <DockPanel>
                        <Button DockPanel.Dock="Right"
                                Margin="12,0,0,0"
                                Click="OnDismissDefaultNamePrompt"
                                Content="Not now"
                                Style="{StaticResource SecondaryButtonStyle}" />
                        <Button DockPanel.Dock="Right"
                                Click="OnRenameDefaultName"
                                Content="{Binding DefaultNamePromptActionLabel}"
                                Style="{StaticResource PrimaryButtonStyle}" />
                        <TextBlock VerticalAlignment="Center"
                                   Text="{Binding DefaultNamePromptMessage}"
                                   TextWrapping="Wrap" />
                    </DockPanel>
                </Border>

                <Grid Grid.Row="2" Margin="18">
                    <views:PortfolioView Visibility="{Binding IsPortfolio, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <views:ProjectDashboardView Visibility="{Binding IsProjectDashboard, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <views:TrackerWorkspaceView x:Name="WorkspaceView"
                                                DataContext="{Binding CurrentWorkspace}"
                                                Visibility="{Binding DataContext.IsTrackerWorkspace, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <views:HelpSqlView Visibility="{Binding IsHelpSql, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <views:SettingsView Visibility="{Binding IsSettings, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>

                <Border Grid.Row="3"
                        MaxHeight="140"
                        Padding="12,8"
                        Background="{DynamicResource Brush.Surface.Alert}"
                        BorderBrush="{DynamicResource Brush.Accent}"
                        BorderThickness="0,1,0,0"
                        AutomationProperties.LiveSetting="Polite"
                        Visibility="{Binding HasNotification, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <DockPanel>
                        <Button DockPanel.Dock="Right"
                                Margin="12,0,0,0"
                                AutomationProperties.Name="Dismiss notification"
                                Click="OnDismissNotification"
                                Content="Dismiss"
                                Style="{StaticResource SecondaryButtonStyle}" />
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <TextBlock VerticalAlignment="Center"
                                       Text="{Binding NotificationMessage}"
                                       TextWrapping="Wrap" />
                        </ScrollViewer>
                    </DockPanel>
                </Border>

                <Border Grid.Row="4"
                        Padding="12,8"
                        AutomationProperties.LiveSetting="Polite"
                        Background="{DynamicResource Brush.Surface.Subtle}"
                        Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <ProgressBar Width="100" Height="5" IsIndeterminate="True" />
                        <TextBlock Margin="12,0,0,0" Text="{Binding BusyMessage}" />
                        <Button Margin="12,0,0,0"
                                AutomationProperties.Name="Cancel synchronization"
                                Click="OnCancelSynchronization"
                                Content="Cancel"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Visibility="{Binding CanCancelSynchronization, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <views:CatalogModalView Panel.ZIndex="20"
                                DataContext="{Binding Catalog}"
                                Visibility="{Binding DataContext.Catalog.IsOpen, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
</Window>
