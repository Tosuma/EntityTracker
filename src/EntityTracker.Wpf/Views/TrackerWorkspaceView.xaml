<UserControl x:Class="EntityTracker.Wpf.Views.TrackerWorkspaceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:configuration="clr-namespace:EntityTracker.Infrastructure.Configuration;assembly=EntityTracker.Infrastructure"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        xmlns:controls="clr-namespace:EntityTracker.Wpf.Controls"
        xmlns:domain="clr-namespace:EntityTracker.Domain;assembly=EntityTracker.Domain"
        xmlns:workflow="clr-namespace:EntityTracker.Application.Workflow;assembly=EntityTracker.Application"
        xmlns:reporting="clr-namespace:EntityTracker.Reporting;assembly=EntityTracker.Reporting"
        xmlns:viewModels="clr-namespace:EntityTracker.Wpf.ViewModels"
        mc:Ignorable="d"
        Background="{DynamicResource Brush.Surface.Page}"
        Foreground="{DynamicResource Brush.Text.Primary}"
        PreviewKeyDown="OnWindowPreviewKeyDown"
        PreviewMouseDown="OnWindowPreviewMouseDown">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <Style x:Key="SummaryCardStyle" TargetType="Border">
            <Setter Property="Margin" Value="0,0,10,8" />
            <Setter Property="Padding" Value="16,12" />
            <Setter Property="MinWidth" Value="128" />
            <Setter Property="Background" Value="{DynamicResource Brush.Surface.Card}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Default}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="5" />
        </Style>

        <Style x:Key="SummaryFilterButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="0,0,10,8" />
            <Setter Property="Padding" Value="16,12" />
            <Setter Property="MinWidth" Value="128" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Background" Value="{DynamicResource Brush.Surface.Card}" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Default}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Card"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource Brush.Border.Strong}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource Brush.Brand.Green100}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Cursor" Value="Arrow" />
                                <Setter TargetName="Card" Property="Opacity" Value="0.48" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EntityNameButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="Padding" Value="3,1" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <DataTemplate x:Key="DevelopmentStatusBadgeTemplate">
            <Border x:Name="Badge"
                    Padding="8,3"
                    HorizontalAlignment="Left"
                    Background="{DynamicResource Brush.Status.NotStarted}"
                    CornerRadius="10">
                <TextBlock x:Name="BadgeText"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Brush.Brand.DarkGreen}"
                           Text="Not started" />
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding}" Value="{x:Static domain:DevelopmentStatus.InProgress}">
                    <Setter TargetName="Badge" Property="Background" Value="{DynamicResource Brush.Status.InProgress}" />
                    <Setter TargetName="BadgeText" Property="Foreground" Value="{DynamicResource Brush.Text.OnDark}" />
                    <Setter TargetName="BadgeText" Property="Text" Value="In progress" />
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="{x:Static domain:DevelopmentStatus.ReworkNeeded}">
                    <Setter TargetName="Badge" Property="Background" Value="{DynamicResource Brush.Status.ReworkNeeded}" />
                    <Setter TargetName="BadgeText" Property="Text" Value="Rework needed" />
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="{x:Static domain:DevelopmentStatus.DevelopmentCompleted}">
                    <Setter TargetName="Badge" Property="Background" Value="{DynamicResource Brush.Status.DevelopmentCompleted}" />
                    <Setter TargetName="BadgeText" Property="Text" Value="Dev. completed" />
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="{x:Static domain:DevelopmentStatus.Reconciled}">
                    <Setter TargetName="Badge" Property="Background" Value="{DynamicResource Brush.Status.Reconciled}" />
                    <Setter TargetName="BadgeText" Property="Foreground" Value="{DynamicResource Brush.Text.OnDark}" />
                    <Setter TargetName="BadgeText" Property="Text" Value="Reconciled" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <DataTemplate x:Key="WorkStatusBadgeTemplate">
            <Border x:Name="Badge"
                    Padding="8,3"
                    HorizontalAlignment="Left"
                    Background="{DynamicResource Brush.Surface.Subtle}"
                    BorderBrush="{DynamicResource Brush.Border.Medium}"
                    BorderThickness="1"
                    CornerRadius="10">
                <TextBlock x:Name="BadgeText"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Brush.Text.Primary}"
                           Text="Archived" />
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding}" Value="{x:Static workflow:EntityWorkflowState.Ready}">
                    <Setter TargetName="Badge" Property="Background" Value="{DynamicResource Brush.Status.Ready}" />
                    <Setter TargetName="Badge" Property="BorderThickness" Value="0" />
                    <Setter TargetName="BadgeText" Property="Foreground" Value="{DynamicResource Brush.Text.OnDark}" />
                    <Setter TargetName="BadgeText" Property="Text" Value="Ready" />
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="{x:Static workflow:EntityWorkflowState.Blocked}">
                    <Setter TargetName="Badge" Property="Background" Value="{DynamicResource Brush.Status.Blocked}" />
                    <Setter TargetName="Badge" Property="BorderThickness" Value="0" />
                    <Setter TargetName="BadgeText" Property="Foreground" Value="{DynamicResource Brush.Brand.DarkGreen}" />
                    <Setter TargetName="BadgeText" Property="Text" Value="Blocked" />
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="{x:Static workflow:EntityWorkflowState.InProgress}">
                    <Setter TargetName="Badge" Property="Background" Value="{DynamicResource Brush.Status.InProgress}" />
                    <Setter TargetName="Badge" Property="BorderThickness" Value="0" />
                    <Setter TargetName="BadgeText" Property="Foreground" Value="{DynamicResource Brush.Text.OnDark}" />
                    <Setter TargetName="BadgeText" Property="Text" Value="In progress" />
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="{x:Static workflow:EntityWorkflowState.ReworkNeeded}">
                    <Setter TargetName="Badge" Property="Background" Value="{DynamicResource Brush.Status.ReworkNeeded}" />
                    <Setter TargetName="Badge" Property="BorderThickness" Value="0" />
                    <Setter TargetName="BadgeText" Property="Foreground" Value="{DynamicResource Brush.Brand.DarkGreen}" />
                    <Setter TargetName="BadgeText" Property="Text" Value="Rework needed" />
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="{x:Static workflow:EntityWorkflowState.DevelopmentCompleted}">
                    <Setter TargetName="Badge" Property="Background" Value="{DynamicResource Brush.Status.DevelopmentCompleted}" />
                    <Setter TargetName="Badge" Property="BorderThickness" Value="0" />
                    <Setter TargetName="BadgeText" Property="Foreground" Value="{DynamicResource Brush.Brand.DarkGreen}" />
                    <Setter TargetName="BadgeText" Property="Text" Value="Dev. completed" />
                </DataTrigger>
                <DataTrigger Binding="{Binding}" Value="{x:Static workflow:EntityWorkflowState.Reconciled}">
                    <Setter TargetName="Badge" Property="Background" Value="{DynamicResource Brush.Status.Reconciled}" />
                    <Setter TargetName="Badge" Property="BorderThickness" Value="0" />
                    <Setter TargetName="BadgeText" Property="Foreground" Value="{DynamicResource Brush.Text.OnDark}" />
                    <Setter TargetName="BadgeText" Property="Text" Value="Reconciled" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <Style TargetType="DataGrid" BasedOn="{StaticResource EntityTrackerDataGridStyle}">
            <EventSetter Event="PreviewMouseWheel" Handler="OnDataGridPreviewMouseWheel" />
            <EventSetter Event="PreviewMouseMove" Handler="OnDataGridPreviewMouseMove" />
            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnDataGridPreviewMouseLeftButtonDown" />
            <EventSetter Event="PreviewMouseLeftButtonUp" Handler="OnDataGridPreviewMouseLeftButtonUp" />
            <EventSetter Event="MouseLeave" Handler="OnDataGridMouseLeave" />
            <EventSetter Event="LostMouseCapture" Handler="OnDataGridLostMouseCapture" />
        </Style>
        <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
            <Setter Property="Height" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </UserControl.Resources>

    <UserControl.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding Editor.CancelCommand}" />
    </UserControl.InputBindings>

    <Grid>
    <DockPanel x:Name="ApplicationContent">
        <DockPanel.Style>
            <Style TargetType="DockPanel">
                <Setter Property="IsHitTestVisible" Value="True" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Editor.IsOpen}" Value="True">
                        <Setter Property="IsHitTestVisible" Value="False" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </DockPanel.Style>
        <Border DockPanel.Dock="Top"
                Visibility="Collapsed"
                Padding="26,18"
                Background="{DynamicResource Brush.Surface.Primary}">
            <StackPanel>
                <TextBlock Foreground="{DynamicResource Brush.Text.OnDark}"
                           FontSize="26"
                           FontWeight="SemiBold"
                           Text="EntityTracker" />
                <TextBlock Margin="0,4,0,0"
                           Foreground="{DynamicResource Brush.Text.OnDarkMuted}"
                           Text="Dependency-safe implementation overview" />
            </StackPanel>
        </Border>

        <Border DockPanel.Dock="Bottom"
                Padding="18,10"
                Background="{DynamicResource Brush.Surface.Subtle}"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <ProgressBar Width="110"
                             Height="5"
                             Margin="0,0,12,0"
                             IsIndeterminate="True" />
                <TextBlock VerticalAlignment="Center"
                           Foreground="{DynamicResource Brush.Text.Primary}"
                           Text="{Binding BusyMessage}" />
            </StackPanel>
        </Border>

        <TabControl Margin="18"
                    SelectedValue="{Binding SelectedTab}"
                    SelectedValuePath="Tag">
            <TabItem Header="Overview" Tag="{x:Static viewModels:MainWindowTab.Overview}">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <DockPanel Margin="0,0,0,14">
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                            <TextBlock Margin="0,0,12,0"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="{Binding OverviewSearchResultSummary}" />
                            <Button Margin="0,0,8,0"
                                    Command="{Binding ActiveTable.ClearAllFiltersCommand}"
                                    Content="Clear filters &amp; sort"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                            <Button Margin="0,0,8,0"
                                    Command="{Binding OpenOverviewSearchCommand}"
                                    Click="OnOpenOverviewSearchClick"
                                    Content="Search"
                                    ToolTip="Search entities (Ctrl+F)"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                            <Button Command="{Binding RefreshCommand}"
                                    Content="Refresh"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                        </StackPanel>
                        <StackPanel>
                            <TextBlock FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                       Text="Persisted entities" />
                            <TextBlock Margin="0,3,0,0"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="Entities are grouped by priority while dependencies remain before the entities that use them." />
                        </StackPanel>
                    </DockPanel>

                    <WrapPanel Grid.Row="1" Margin="0,0,0,6">

                        <Button AutomationProperties.Name="Show all active entities"
                                Command="{Binding ActiveTable.ClearAllFiltersCommand}"
                                ToolTip="Show all active entities">
                            <Button.Style>
                                <Style BasedOn="{StaticResource SummaryFilterButtonStyle}"
                                       TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTotalSummarySelected}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource Brush.Surface.Selected}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Brand.Green100}" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel>
                                <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" Text="Total" />
                                <TextBlock Margin="0,3,0,0"
                                           FontSize="22"
                                           FontWeight="SemiBold"
                                           Text="{Binding TotalEntityCount}" />
                            </StackPanel>
                        </Button>
                        <Button AutomationProperties.Name="Show not started entities"
                                Command="{Binding SelectOverviewStatusCommand}"
                                CommandParameter="{x:Static domain:DevelopmentStatus.NotStarted}"
                                ToolTip="Show not started entities">
                            <Button.Style>
                                <Style BasedOn="{StaticResource SummaryFilterButtonStyle}"
                                       TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsNotStartedSummarySelected}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource Brush.Surface.Selected}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Brand.Green100}" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel>
                                <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" Text="Not started" />
                                <TextBlock Margin="0,3,0,0"
                                           FontSize="22"
                                           FontWeight="SemiBold"
                                           Text="{Binding NotStartedCount}" />
                            </StackPanel>
                        </Button>
                        <Button AutomationProperties.Name="Show in progress entities"
                                Command="{Binding SelectOverviewStatusCommand}"
                                CommandParameter="{x:Static domain:DevelopmentStatus.InProgress}"
                                ToolTip="Show in progress entities">
                            <Button.Style>
                                <Style BasedOn="{StaticResource SummaryFilterButtonStyle}"
                                       TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsInProgressSummarySelected}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource Brush.Surface.Selected}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Brand.Green100}" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel>
                                <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" Text="In progress" />
                                <TextBlock Margin="0,3,0,0"
                                           FontSize="22"
                                           FontWeight="SemiBold"
                                           Text="{Binding InProgressCount}" />
                            </StackPanel>
                        </Button>
                        <Button AutomationProperties.Name="Show rework needed entities"
                                Command="{Binding SelectOverviewStatusCommand}"
                                CommandParameter="{x:Static domain:DevelopmentStatus.ReworkNeeded}"
                                ToolTip="Show rework needed entities">
                            <Button.Style>
                                <Style BasedOn="{StaticResource SummaryFilterButtonStyle}"
                                       TargetType="Button">
                                    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Accent}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsReworkNeededSummarySelected}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource Brush.Surface.Selected}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Accent}" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="9"
                                            Height="9"
                                            Margin="0,0,6,0"
                                            VerticalAlignment="Center"
                                            Background="{DynamicResource Brush.Status.ReworkNeeded}"
                                            CornerRadius="5" />
                                    <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" Text="Rework needed" />
                                </StackPanel>
                                <TextBlock Margin="0,3,0,0"
                                           FontSize="22"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource Brush.Text.Primary}"
                                           Text="{Binding ReworkNeededCount}" />
                            </StackPanel>
                        </Button>

                        <Button AutomationProperties.Name="Show development completed entities"
                                Command="{Binding SelectOverviewStatusCommand}"
                                CommandParameter="{x:Static domain:DevelopmentStatus.DevelopmentCompleted}"
                                ToolTip="Show development completed entities">
                            <Button.Style>
                                <Style BasedOn="{StaticResource SummaryFilterButtonStyle}"
                                       TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDevelopmentCompletedSummarySelected}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource Brush.Surface.Selected}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Brand.Green100}" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel>
                                <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" Text="Dev. completed" />
                                <TextBlock Margin="0,3,0,0"
                                           FontSize="22"
                                           FontWeight="SemiBold"
                                           Text="{Binding DevelopmentCompletedCount}" />
                            </StackPanel>
                        </Button>

                        <Button AutomationProperties.Name="Show reconciled entities"
                                Command="{Binding SelectOverviewStatusCommand}"
                                CommandParameter="{x:Static domain:DevelopmentStatus.Reconciled}"
                                ToolTip="Show reconciled entities">
                            <Button.Style>
                                <Style BasedOn="{StaticResource SummaryFilterButtonStyle}"
                                       TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsReconciledSummarySelected}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource Brush.Surface.Selected}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Brand.Green100}" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel>
                                <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" Text="Reconciled" />
                                <TextBlock Margin="0,3,0,0"
                                           FontSize="22"
                                           FontWeight="SemiBold"
                                           Text="{Binding ReconciledCount}" />
                            </StackPanel>
                        </Button>

                        <Border Margin="0,0,0,8"
                                Style="{StaticResource SummaryCardStyle}">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="{Binding ImplementedPercentage, StringFormat={}{0:F0}% implemented}" />
                                <Grid Height="10" Margin="0,9,0,0">
                                    <ProgressBar Foreground="{DynamicResource Brush.Status.DevelopmentCompleted}"
                                                 Maximum="100"
                                                 Value="{Binding ImplementedPercentage, Mode=OneWay}" />
                                    <ProgressBar Background="Transparent"
                                                 Foreground="{DynamicResource Brush.Status.ReworkNeeded}"
                                                 Maximum="100"
                                                 Value="{Binding ReconciledAndReworkPercentage, Mode=OneWay}" />
                                    <ProgressBar Background="Transparent"
                                                 Foreground="{DynamicResource Brush.Status.Reconciled}"
                                                 Maximum="100"
                                                 Value="{Binding ReconciledPercentage, Mode=OneWay}" />
                                </Grid>
                                <WrapPanel Margin="0,6,0,0">
                                    <StackPanel Margin="0,0,10,0" Orientation="Horizontal">
                                        <Border Width="8"
                                                Height="8"
                                                Margin="0,0,4,0"
                                                VerticalAlignment="Center"
                                                Background="{DynamicResource Brush.Status.DevelopmentCompleted}"
                                                CornerRadius="4" />
                                        <TextBlock FontSize="11"
                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   Text="Dev. completed" />
                                    </StackPanel>
                                    <StackPanel Margin="0,0,10,0" Orientation="Horizontal">
                                        <Border Width="8"
                                                Height="8"
                                                Margin="0,0,4,0"
                                                VerticalAlignment="Center"
                                                Background="{DynamicResource Brush.Status.ReworkNeeded}"
                                                CornerRadius="4" />
                                        <TextBlock FontSize="11"
                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   Text="Rework needed" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Width="8"
                                                Height="8"
                                                Margin="0,0,4,0"
                                                VerticalAlignment="Center"
                                                Background="{DynamicResource Brush.Status.Reconciled}"
                                                CornerRadius="4" />
                                        <TextBlock FontSize="11"
                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   Text="Reconciled" />
                                    </StackPanel>
                                </WrapPanel>
                            </StackPanel>
                        </Border>
                    </WrapPanel>

                    <Border Grid.Row="2"
                            Margin="0,0,0,12"
                            Padding="12"
                            Background="{DynamicResource Brush.Surface.Card}"
                            BorderBrush="{DynamicResource Brush.Border.Medium}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Visibility="{Binding IsOverviewSearchOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="OverviewSearchTextBox"
                                     MinWidth="280"
                                     Padding="9,7"
                                     ToolTip="Search active entities"
                                     Text="{Binding OverviewSearchQuery, UpdateSourceTrigger=PropertyChanged}" />
                            <Button Grid.Column="1"
                                    Margin="8,0,0,0"
                                    Command="{Binding ClearOverviewSearchCommand}"
                                    Content="Clear"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                            <Button Grid.Column="2"
                                    Margin="8,0,0,0"
                                    Command="{Binding CloseOverviewSearchCommand}"
                                    Content="Close search"
                                    Style="{StaticResource SecondaryButtonStyle}" />

                            <CheckBox Grid.Row="1"
                                      Margin="0,9,0,0"
                                      VerticalAlignment="Center"
                                      Content="Search dependency names"
                                      IsEnabled="{Binding ActiveTable.CanSearchDependencies}"
                                      IsChecked="{Binding SearchOverviewDependencies}" />
                        </Grid>
                    </Border>

                    <Border x:Name="BulkStatusToolbar"
                            Grid.Row="3"
                            Margin="0,0,0,12"
                            Padding="12"
                            Background="{DynamicResource Brush.Surface.Subtle}"
                            BorderBrush="{DynamicResource Brush.Border.Medium}"
                            BorderThickness="1"
                            CornerRadius="4">
                        <DockPanel LastChildFill="False">
                            <TextBlock Margin="0,0,18,0"
                                       VerticalAlignment="Center"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                       Text="{Binding BulkSelectionSummary}" />
                            <TextBlock Margin="0,0,7,0"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="Set status to" />
                            <ComboBox MinWidth="155"
                                      Margin="0,0,8,0"
                                      AutomationProperties.Name="Bulk target status"
                                      DisplayMemberPath="DisplayName"
                                      ItemsSource="{Binding BulkStatusOptions}"
                                      SelectedValue="{Binding SelectedBulkStatus}"
                                      SelectedValuePath="Value" />
                            <Button AutomationProperties.Name="Apply status to selected entities"
                                    Command="{Binding ApplyBulkStatusCommand}"
                                    Content="Apply status"
                                    Style="{StaticResource PrimaryButtonStyle}" />
                        </DockPanel>
                    </Border>

                    <StackPanel Grid.Row="4">
                    <Border Margin="0,0,0,12"
                            Padding="12"
                            Background="{DynamicResource Brush.Surface.Subtle}"
                            BorderBrush="{DynamicResource Brush.Border.Strong}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Visibility="{Binding HasOperationMessage, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Foreground="{DynamicResource Brush.Text.Primary}"
                                   Text="{Binding OperationMessage}"
                                   TextWrapping="Wrap" />
                    </Border>

                    <Border
                            Margin="0,0,0,12"
                            MaxHeight="120"
                            Padding="12"
                            Background="{DynamicResource Brush.Surface.Alert}"
                            BorderBrush="{DynamicResource Brush.Accent}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Visibility="{Binding HasOverviewError, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                      VerticalScrollBarVisibility="Auto">
                            <TextBlock Foreground="{DynamicResource Brush.Text.Primary}"
                                       Text="{Binding OverviewErrorMessage}"
                                       TextWrapping="Wrap" />
                        </ScrollViewer>
                    </Border>
                    </StackPanel>

                    <Grid Grid.Row="5">
                        <DataGrid x:Name="OverviewDataGrid"
                                  ItemsSource="{Binding OverviewItems}"
                                  MouseDoubleClick="OnEntityDataGridMouseDoubleClick"
                                  SelectionChanged="OnOverviewSelectionChanged"
                                  SelectionMode="Extended"
                                  SelectionUnit="FullRow">
                            <DataGrid.Columns>
                                <DataGridTextColumn Width="72" Binding="{Binding Priority}" Header="Priority" />
                                <DataGridTextColumn Width="70" Binding="{Binding Rank}" Header="Rank" />
                                <DataGridTemplateColumn Width="2.2*" Header="Entity">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                                <Button AutomationProperties.Name="{Binding SourceName, StringFormat=View details for {0}}"
                                                        Command="{Binding DataContext.OpenEntityDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        PreviewMouseLeftButtonDown="OnEntityNamePreviewMouseLeftButtonDown"
                                                        Style="{StaticResource EntityNameButtonStyle}">
                                                    <TextBlock Text="{Binding SourceName}"
                                                               TextDecorations="Underline" />
                                                </Button>
                                                <Button Width="24"
                                                        Height="24"
                                                        Margin="6,0,0,0"
                                                        Padding="2"
                                                        VerticalAlignment="Center"
                                                        AutomationProperties.Name="{Binding GraphIssueTitle}"
                                                        AutomationProperties.HelpText="{Binding GraphIssueDescription}"
                                                        Background="Transparent"
                                                        BorderThickness="0"
                                                        Cursor="Hand"
                                                        ToolTip="{Binding GraphIssueTitle}"
                                                        Visibility="{Binding HasGraphIssue, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                        Click="OnContextMenuButtonClick">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Accent}" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsDirectlyUnresolved}" Value="True">
                                                                    <Setter Property="Foreground" Value="{DynamicResource Brush.Accent}" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                    <Viewbox Width="18"
                                                             Height="18"
                                                             IsHitTestVisible="False">
                                                        <Canvas Width="24" Height="24">
                                                            <Ellipse Width="20"
                                                                     Height="20"
                                                                     Canvas.Left="2"
                                                                     Canvas.Top="2"
                                                                     Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                                     StrokeThickness="2" />
                                                            <Path Data="M12,6 L12,14"
                                                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                                  StrokeEndLineCap="Round"
                                                                  StrokeStartLineCap="Round"
                                                                  StrokeThickness="2" />
                                                            <Ellipse Width="2.5"
                                                                     Height="2.5"
                                                                     Canvas.Left="10.75"
                                                                     Canvas.Top="17"
                                                                     Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                                                        </Canvas>
                                                    </Viewbox>
                                                    <Button.ContextMenu>
                                                        <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                                            <MenuItem Focusable="False" IsHitTestVisible="False">
                                                                <MenuItem.Template>
                                                                    <ControlTemplate TargetType="MenuItem">
                                                                        <ContentPresenter ContentSource="Header" />
                                                                    </ControlTemplate>
                                                                </MenuItem.Template>
                                                                <MenuItem.Header>
                                                                    <StackPanel Width="320" Margin="12">
                                                                        <TextBlock FontSize="15"
                                                                                   FontWeight="SemiBold"
                                                                                   Foreground="{DynamicResource Brush.Text.Primary}"
                                                                                   Text="{Binding GraphIssueTitle}" />
                                                                        <TextBlock Margin="0,8,0,0"
                                                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                                                   Text="{Binding GraphIssueDescription}"
                                                                                   TextWrapping="Wrap" />
                                                                        <TextBlock Margin="0,10,0,0"
                                                                                   FontWeight="SemiBold"
                                                                                   Foreground="{DynamicResource Brush.Text.Primary}"
                                                                                   Text="{Binding GraphIssueNames}"
                                                                                   TextWrapping="Wrap" />
                                                                    </StackPanel>
                                                                </MenuItem.Header>
                                                            </MenuItem>
                                                        </ContextMenu>
                                                    </Button.ContextMenu>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Width="140">
                                    <DataGridTemplateColumn.Header>
                                        <controls:FilterableColumnHeader Filter="{Binding DataContext.ActiveTable.WorkStatusFilter, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                    </DataGridTemplateColumn.Header>
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ContentControl AutomationProperties.Name="{Binding WorkStatus, StringFormat=Work status: {0}}"
                                                            Content="{Binding WorkflowState}"
                                                            ContentTemplate="{StaticResource WorkStatusBadgeTemplate}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Width="180">
                                    <DataGridTemplateColumn.Header>
                                        <controls:FilterableColumnHeader Filter="{Binding DataContext.ActiveTable.StatusFilter, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                    </DataGridTemplateColumn.Header>
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ContentControl AutomationProperties.Name="{Binding Status, StringFormat=Development status: {0}}"
                                                            Content="{Binding DevelopmentStatus}"
                                                            ContentTemplate="{StaticResource DevelopmentStatusBadgeTemplate}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Width="150"
                                                    Binding="{Binding ResponsibleDeveloperDisplay}">
                                    <DataGridTextColumn.Header>
                                        <controls:FilterableColumnHeader Filter="{Binding DataContext.ActiveTable.ResponsibleDeveloperFilter, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                    </DataGridTextColumn.Header>
                                </DataGridTextColumn>
                                <DataGridTextColumn Width="145"
                                                    Binding="{Binding GroupNameDisplay}">
                                    <DataGridTextColumn.Header>
                                        <controls:FilterableColumnHeader Filter="{Binding DataContext.ActiveTable.GroupFilter, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                    </DataGridTextColumn.Header>
                                </DataGridTextColumn>
                                <DataGridTextColumn Width="2.2*"
                                                    Binding="{Binding BlockersDisplay}">
                                    <DataGridTextColumn.Header>
                                        <TextBlock AutomationProperties.HelpText="Unresolved or not-yet-implemented direct dependencies"
                                                   Text="Blockers"
                                                   ToolTip="Unresolved or not-yet-implemented direct dependencies" />
                                    </DataGridTextColumn.Header>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Width="76" Header="Actions">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Width="30"
                                                    Height="28"
                                                    Padding="0"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Content="⋮"
                                                    Cursor="Hand"
                                                    FontSize="19"
                                                    ToolTip="Entity actions"
                                                    AutomationProperties.Name="Entity actions"
                                                    Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    Click="OnContextMenuButtonClick">
                                                <Button.ContextMenu>
                                                    <ContextMenu>
                                                        <MenuItem Header="View details"
                                                                  Command="{Binding PlacementTarget.Tag.OpenEntityDetailsCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                                        <MenuItem Header="{Binding PlacementTarget.DataContext.ActionLabel, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                                  Command="{Binding PlacementTarget.Tag.EditOverviewEntityCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                                    </ContextMenu>
                                                </Button.ContextMenu>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Background="{DynamicResource Brush.Surface.Subtle}"
                                BorderBrush="{DynamicResource Brush.Border.Default}"
                                BorderThickness="1"
                                Visibility="{Binding ShowOverviewEmptyState, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock HorizontalAlignment="Center"
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="No active entities" />
                                <TextBlock Margin="0,7,0,0"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="Add an entity manually or open Schema Synchronization to import a CSV." />
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource Brush.Surface.Subtle}"
                                BorderBrush="{DynamicResource Brush.Border.Default}"
                                BorderThickness="1"
                                Visibility="{Binding ShowOverviewSearchEmptyState, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock HorizontalAlignment="Center"
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="No matching entities" />
                                <TextBlock Margin="0,7,0,12"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="Try another search or clear one of the active column filters."
                                           TextWrapping="Wrap" />
                                <WrapPanel HorizontalAlignment="Center">
                                    <Button Margin="0,0,8,0"
                                            Command="{Binding ClearOverviewSearchCommand}"
                                            Content="Clear search"
                                            Style="{StaticResource SecondaryButtonStyle}" />
                                    <Button Command="{Binding ActiveTable.ClearAllFiltersCommand}"
                                            Content="Clear filters &amp; sort"
                                            Style="{StaticResource SecondaryButtonStyle}" />
                                </WrapPanel>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Archived" Tag="{x:Static viewModels:MainWindowTab.Archived}">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <DockPanel Margin="0,0,0,14">
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                            <Button Margin="0,0,8,0"
                                    Command="{Binding ArchivedTable.ClearAllFiltersCommand}"
                                    Content="Clear filters &amp; sort"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                            <Button Margin="0,0,8,0"
                                    Command="{Binding ArchivedTable.OpenSearchCommand}"
                                    Click="OnOpenOverviewSearchClick"
                                    Content="Search"
                                    ToolTip="Search archived entities (Ctrl+F)"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                            <Button Command="{Binding RefreshCommand}"
                                    Content="Refresh"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                        </StackPanel>
                        <StackPanel>
                            <TextBlock FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                       Text="Archived entities" />
                            <TextBlock Margin="0,3,0,0"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="Inspect archived metadata or restore an entity to the active dependency graph." />
                        </StackPanel>
                    </DockPanel>

                    <Border Grid.Row="1"
                            Margin="0,0,0,12"
                            Padding="12"
                            Background="{DynamicResource Brush.Surface.Card}"
                            BorderBrush="{DynamicResource Brush.Border.Medium}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Visibility="{Binding ArchivedTable.IsSearchOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="ArchivedSearchTextBox"
                                     MinWidth="280"
                                     Padding="9,7"
                                     ToolTip="Search archived entities"
                                     Text="{Binding ArchivedTable.SearchQuery, UpdateSourceTrigger=PropertyChanged}" />
                            <Button Grid.Column="1"
                                    Margin="8,0,0,0"
                                    Command="{Binding ArchivedTable.ClearSearchCommand}"
                                    Content="Clear"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                            <Button Grid.Column="2"
                                    Margin="8,0,0,0"
                                    Command="{Binding ArchivedTable.CloseSearchCommand}"
                                    Content="Close search"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                        </Grid>
                    </Border>

                    <StackPanel Grid.Row="2">
                        <TextBlock Margin="0,0,0,8"
                                   HorizontalAlignment="Right"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   Text="{Binding ArchivedSearchResultSummary}" />
                        <Border Margin="0,0,0,12"
                                MaxHeight="120"
                                Padding="12"
                                Background="{DynamicResource Brush.Surface.Alert}"
                                BorderBrush="{DynamicResource Brush.Accent}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Visibility="{Binding HasOverviewError, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto">
                                <TextBlock Foreground="{DynamicResource Brush.Text.Primary}"
                                           Text="{Binding OverviewErrorMessage}"
                                           TextWrapping="Wrap" />
                            </ScrollViewer>
                        </Border>
                    </StackPanel>

                    <Grid Grid.Row="3">
                        <DataGrid x:Name="ArchivedDataGrid"
                                  ItemsSource="{Binding ArchivedItems}"
                                  MouseDoubleClick="OnEntityDataGridMouseDoubleClick"
                                  SelectionMode="Single"
                                  SelectionUnit="FullRow">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Width="2.2*" Header="Entity">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button AutomationProperties.Name="{Binding SourceName, StringFormat=View details for {0}}"
                                                    Command="{Binding DataContext.OpenEntityDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    PreviewMouseLeftButtonDown="OnEntityNamePreviewMouseLeftButtonDown"
                                                    Style="{StaticResource EntityNameButtonStyle}">
                                                <TextBlock Text="{Binding SourceName}"
                                                           TextDecorations="Underline" />
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Width="180">
                                    <DataGridTemplateColumn.Header>
                                        <controls:FilterableColumnHeader Filter="{Binding DataContext.ArchivedTable.StatusFilter, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                    </DataGridTemplateColumn.Header>
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ContentControl AutomationProperties.Name="{Binding Status, StringFormat=Development status: {0}}"
                                                            Content="{Binding DevelopmentStatus}"
                                                            ContentTemplate="{StaticResource DevelopmentStatusBadgeTemplate}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Width="150" Binding="{Binding ResponsibleDeveloperDisplay}">
                                    <DataGridTextColumn.Header>
                                        <controls:FilterableColumnHeader Filter="{Binding DataContext.ArchivedTable.ResponsibleDeveloperFilter, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                    </DataGridTextColumn.Header>
                                </DataGridTextColumn>
                                <DataGridTextColumn Width="145" Binding="{Binding GroupNameDisplay}">
                                    <DataGridTextColumn.Header>
                                        <controls:FilterableColumnHeader Filter="{Binding DataContext.ArchivedTable.GroupFilter, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                    </DataGridTextColumn.Header>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Width="76" Header="Actions">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Width="30"
                                                    Height="28"
                                                    Padding="0"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Content="⋮"
                                                    Cursor="Hand"
                                                    FontSize="19"
                                                    ToolTip="Archived entity actions"
                                                    AutomationProperties.Name="Archived entity actions"
                                                    Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    Click="OnContextMenuButtonClick">
                                                <Button.ContextMenu>
                                                    <ContextMenu>
                                                        <MenuItem Header="View details"
                                                                  Command="{Binding PlacementTarget.Tag.OpenEntityDetailsCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                                        <MenuItem Header="{Binding PlacementTarget.DataContext.ActionLabel, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                                  Command="{Binding PlacementTarget.Tag.EditOverviewEntityCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                                  CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                                    </ContextMenu>
                                                </Button.ContextMenu>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Background="{DynamicResource Brush.Surface.Subtle}"
                                BorderBrush="{DynamicResource Brush.Border.Default}"
                                BorderThickness="1"
                                Visibility="{Binding ShowArchivedEmptyState, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock HorizontalAlignment="Center"
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="No archived entities" />
                                <TextBlock Margin="0,7,0,0"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="Archived entities will appear here and can be inspected or restored." />
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource Brush.Surface.Subtle}"
                                BorderBrush="{DynamicResource Brush.Border.Default}"
                                BorderThickness="1"
                                Visibility="{Binding ShowArchivedSearchEmptyState, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock HorizontalAlignment="Center"
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="No matching archived entities" />
                                <TextBlock Margin="0,7,0,12"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="Try another search or clear one of the active column filters." />
                                <WrapPanel HorizontalAlignment="Center">
                                    <Button Margin="0,0,8,0"
                                            Command="{Binding ArchivedTable.ClearSearchCommand}"
                                            Content="Clear search"
                                            Style="{StaticResource SecondaryButtonStyle}" />
                                    <Button Command="{Binding ArchivedTable.ClearAllFiltersCommand}"
                                            Content="Clear filters &amp; sort"
                                            Style="{StaticResource SecondaryButtonStyle}" />
                                </WrapPanel>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Schema Synchronization" Tag="{x:Static viewModels:MainWindowTab.SchemaSynchronization}">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <DockPanel Margin="0,0,0,12">
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                            <Button Margin="0,0,8,0"
                                    Command="{Binding OpenSqlQueryCommand}"
                                    Content="Get SQL Query"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                            <Button Command="{Binding ImportCsvCommand}"
                                    Content="Choose CSV and Review"
                                    Style="{StaticResource PrimaryButtonStyle}" />
                        </StackPanel>
                        <StackPanel>
                            <TextBlock FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                       Text="Schema synchronization" />
                            <TextBlock Margin="0,3,0,0"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="{Binding Review.SelectedFileName}" />
                        </StackPanel>
                    </DockPanel>

                    <Border Grid.Row="1"
                            Margin="0,0,0,12"
                            Padding="12"
                            Background="{DynamicResource Brush.Surface.Card}"
                            BorderBrush="{DynamicResource Brush.Border.Default}"
                            BorderThickness="1"
                            CornerRadius="4">
                        <StackPanel>
                            <Border Margin="0,0,0,12"
                                    Padding="10"
                                    Background="{DynamicResource Brush.Surface.Subtle}"
                                    BorderBrush="{DynamicResource Brush.Border.Default}"
                                    BorderThickness="1"
                                    CornerRadius="4">
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold"
                                               Foreground="{DynamicResource Brush.Text.Primary}"
                                               Text="{Binding LatestImportHeadline}" />
                                    <TextBlock Margin="0,3,0,0"
                                               Foreground="{DynamicResource Brush.Text.Secondary}"
                                               Text="{Binding LatestImportDetails}"
                                               TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                            <TextBlock FontWeight="SemiBold" Text="Import type" />
                            <RadioButton Margin="0,8,0,0"
                                         GroupName="ImportMode"
                                         IsEnabled="{Binding Review.CanSelectImportMode}"
                                         IsChecked="{Binding Review.IsCompleteImport}"
                                         Content="Complete database snapshot (default)" />
                            <TextBlock Margin="22,2,0,0"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="The CSV contains the entire schema. Existing entities missing from the file will be archived." />
                            <RadioButton Margin="0,9,0,0"
                                         GroupName="ImportMode"
                                         IsEnabled="{Binding Review.CanSelectImportMode}"
                                         IsChecked="{Binding Review.IsPartialImport}"
                                         Content="Partial database snapshot" />
                            <TextBlock Margin="22,2,0,0"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="The CSV contains only part of the schema. Existing entities missing from the file will be left unchanged." />
                            <TextBlock Margin="0,10,0,0"
                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                       FontWeight="SemiBold"
                                       Text="Import type is locked for while reviewing. Cancel the review to choose a different type."
                                       TextWrapping="Wrap"
                                       Visibility="{Binding Review.HasReview, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </StackPanel>
                    </Border>

                    <Grid Grid.Row="2">
                        <ScrollViewer x:Name="SchemaReviewScrollViewer"
                                      HorizontalScrollBarVisibility="Disabled"
                                      PreviewMouseWheel="OnReviewPreviewMouseWheel"
                                      VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <Border Margin="0,0,0,8"
                                        MaxHeight="120"
                                        Padding="10"
                                        Background="{DynamicResource Brush.Surface.Subtle}"
                                        BorderBrush="{DynamicResource Brush.Accent}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Visibility="{Binding Review.HasWarnings, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding Review.Warnings}" />
                                    </ScrollViewer>
                                </Border>

                                <Border Margin="0,0,0,8"
                                        MaxHeight="120"
                                        Padding="10"
                                        Background="{DynamicResource Brush.Surface.Alert}"
                                        BorderBrush="{DynamicResource Brush.Accent}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Visibility="{Binding Review.HasDiagnostics, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding Review.Diagnostics}" />
                                    </ScrollViewer>
                                </Border>

                                <StackPanel Visibility="{Binding Review.HasReview, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Margin="0,0,0,10"
                                               FontWeight="SemiBold"
                                               Text="{Binding Review.ImportModeLabel, StringFormat=Import mode: {0}}" />

                                    <StackPanel Margin="0,0,0,12"
                                                Visibility="{Binding Review.HasNewEntities, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Brand.Green100}" Text="New entities" />
                                        <ItemsControl ItemsSource="{Binding Review.NewEntities}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Margin="0,5,0,0" Padding="10" Background="{DynamicResource Brush.Surface.Card}" BorderBrush="{DynamicResource Brush.Border.Default}" BorderThickness="1">
                                                        <StackPanel>
                                                            <DockPanel>
                                                                <Button DockPanel.Dock="Right"
                                                                        Width="30"
                                                                        Height="28"
                                                                        Padding="0"
                                                                        Background="Transparent"
                                                                        BorderThickness="0"
                                                                        Content="⋮"
                                                                        Cursor="Hand"
                                                                        FontSize="19"
                                                                        ToolTip="Entity actions"
                                                                        AutomationProperties.Name="Entity actions"
                                                                        Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                        Click="OnContextMenuButtonClick">
                                                                    <Button.ContextMenu>
                                                                        <ContextMenu>
                                                                            <MenuItem Header="Edit dependencies"
                                                                                      Command="{Binding PlacementTarget.Tag.EditReviewEntityCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                                                        </ContextMenu>
                                                                    </Button.ContextMenu>
                                                                </Button>
                                                                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="{Binding SourceName, StringFormat=+ {0}}" />
                                                            </DockPanel>
                                                            <TextBlock Margin="16,3,0,0" Foreground="{DynamicResource Brush.Text.Secondary}" Text="{Binding Details}" TextWrapping="Wrap" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <StackPanel Margin="0,0,0,12"
                                                Visibility="{Binding Review.HasChangedEntities, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Primary}" Text="Changed entities" />
                                        <ItemsControl ItemsSource="{Binding Review.ChangedEntities}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Margin="0,5,0,0" Padding="10" Background="{DynamicResource Brush.Surface.Card}" BorderBrush="{DynamicResource Brush.Border.Default}" BorderThickness="1">
                                                        <StackPanel>
                                                            <DockPanel>
                                                                <Button DockPanel.Dock="Right"
                                                                        Width="30"
                                                                        Height="28"
                                                                        Padding="0"
                                                                        Background="Transparent"
                                                                        BorderThickness="0"
                                                                        Content="⋮"
                                                                        Cursor="Hand"
                                                                        FontSize="19"
                                                                        ToolTip="Entity actions"
                                                                        AutomationProperties.Name="Entity actions"
                                                                        Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                        Click="OnContextMenuButtonClick">
                                                                    <Button.ContextMenu>
                                                                        <ContextMenu>
                                                                            <MenuItem Header="Edit dependencies"
                                                                                      Command="{Binding PlacementTarget.Tag.EditReviewEntityCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                                                        </ContextMenu>
                                                                    </Button.ContextMenu>
                                                                </Button>
                                                                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="{Binding SourceName, StringFormat=~ {0}}" />
                                                            </DockPanel>
                                                            <TextBlock Margin="16,3,0,0" Foreground="{DynamicResource Brush.Text.Secondary}" Text="{Binding Details}" TextWrapping="Wrap" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <StackPanel x:Name="MissingReviewSection"
                                                Margin="0,0,0,12"
                                                Visibility="{Binding Review.HasMissingEntities, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Border Padding="8,5"
                                                Background="{DynamicResource Brush.Review.Missing.Header}"
                                                CornerRadius="3">
                                            <TextBlock FontSize="16"
                                                       FontWeight="SemiBold"
                                                       Foreground="{DynamicResource Brush.Brand.DarkGreen}"
                                                       Text="Missing / possibly removed" />
                                        </Border>
                                        <ItemsControl ItemsSource="{Binding Review.MissingEntities}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Margin="0,5,0,0"
                                                            Padding="10"
                                                            Background="{DynamicResource Brush.Review.Missing.Surface}"
                                                            BorderBrush="{DynamicResource Brush.Review.Missing.Border}"
                                                            BorderThickness="1">
                                                        <StackPanel>
                                                            <TextBlock FontWeight="SemiBold" Text="{Binding SourceName, StringFormat=− {0}}" />
                                                            <TextBlock Margin="16,3,0,0" Foreground="{DynamicResource Brush.Text.Primary}" Text="{Binding Details}" TextWrapping="Wrap" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <StackPanel Margin="0,0,0,12"
                                                Visibility="{Binding Review.HasManualOnlyEntities, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock FontSize="16"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource Brush.Brand.Green100}"
                                                   Text="Manual entities not yet in CSV — kept active" />
                                        <ItemsControl ItemsSource="{Binding Review.ManualOnlyEntities}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Margin="0,5,0,0"
                                                            Padding="10"
                                                            Background="{DynamicResource Brush.Surface.Subtle}"
                                                            BorderBrush="{DynamicResource Brush.Border.Strong}"
                                                            BorderThickness="1">
                                                        <StackPanel>
                                                            <TextBlock FontWeight="SemiBold"
                                                                       Text="{Binding SourceName, StringFormat=• {0}}" />
                                                            <TextBlock Margin="16,3,0,0"
                                                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                                                       Text="{Binding Details}"
                                                                       TextWrapping="Wrap" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <StackPanel x:Name="UnresolvedReviewSection"
                                                Margin="0,0,0,12"
                                                Visibility="{Binding Review.HasUnresolvedEntities, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock FontSize="16"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource Brush.Review.Warning.Header}"
                                                   Text="Unresolved dependencies" />
                                        <ItemsControl ItemsSource="{Binding Review.UnresolvedEntities}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Margin="0,5,0,0"
                                                            Padding="10"
                                                            Background="{DynamicResource Brush.Review.Warning.Surface}"
                                                            BorderBrush="{DynamicResource Brush.Review.Warning.Border}"
                                                            BorderThickness="1">
                                                        <StackPanel>
                                                            <TextBlock FontWeight="SemiBold" Text="{Binding SourceName, StringFormat=! {0}}" />
                                                            <TextBlock Margin="16,3,0,0"
                                                                       Foreground="{DynamicResource Brush.Review.Warning.Text}"
                                                                       Text="{Binding Details}"
                                                                       TextWrapping="Wrap" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <StackPanel Margin="0,0,0,12"
                                                Visibility="{Binding Review.HasProgressImpacts, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock FontSize="16"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource Brush.Accent}"
                                                   Text="Completed entities with dependency changes" />
                                        <TextBlock Margin="0,3,0,4"
                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   Text="Choose whether each entity keeps its current status or needs rework before importing."
                                                   TextWrapping="Wrap" />
                                        <ItemsControl ItemsSource="{Binding Review.ProgressImpacts}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Margin="0,5,0,0"
                                                            Padding="10"
                                                            Background="{DynamicResource Brush.Surface.Subtle}"
                                                            BorderBrush="{DynamicResource Brush.Accent}"
                                                            BorderThickness="1">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <StackPanel>
                                                                <TextBlock FontWeight="SemiBold" Text="{Binding SourceName}" />
                                                                <TextBlock Margin="0,3,0,0"
                                                                           Foreground="{DynamicResource Brush.Text.Primary}"
                                                                           Text="{Binding CurrentStatus, StringFormat=Current: {0}}" />
                                                                <TextBlock Margin="0,3,0,0"
                                                                           FontWeight="SemiBold"
                                                                           Foreground="{DynamicResource Brush.Text.Primary}"
                                                                           Text="{Binding DecisionText}" />
                                                            </StackPanel>
                                                            <StackPanel Grid.Column="1"
                                                                        Margin="12,0,0,0"
                                                                        VerticalAlignment="Center"
                                                                        Orientation="Horizontal">
                                                                <Button Margin="0,0,8,0"
                                                                        Command="{Binding DataContext.KeepSynchronizationStatusCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                        CommandParameter="{Binding}"
                                                                        Content="Keep current status"
                                                                        Style="{StaticResource SecondaryButtonStyle}" />
                                                                <Button Command="{Binding DataContext.MarkSynchronizationReworkCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                        CommandParameter="{Binding}"
                                                                        Content="Mark Rework needed"
                                                                        Style="{StaticResource SecondaryButtonStyle}" />
                                                            </StackPanel>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <Border Padding="10" Background="{DynamicResource Brush.Surface.Subtle}" BorderBrush="{DynamicResource Brush.Border.Strong}" BorderThickness="1">
                                        <TextBlock Foreground="{DynamicResource Brush.Text.Primary}"
                                                   Text="{Binding Review.UnchangedEntityCount, StringFormat=✓ {0} imported entities unchanged}" />
                                    </Border>
                                </StackPanel>

                                <Border Padding="20"
                                        Background="{DynamicResource Brush.Surface.Subtle}"
                                        BorderBrush="{DynamicResource Brush.Border.Default}"
                                        BorderThickness="1"
                                        Visibility="{Binding Review.ShowEmptyState, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <StackPanel>
                                        <TextBlock HorizontalAlignment="Center"
                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   Text="Choose an import type, then select a CSV to review. Nothing is saved until Import Changes." />
                                        <TextBlock Margin="0,5,0,0"
                                                   HorizontalAlignment="Center"
                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   Text="Need an export query? Get it separately with Get SQL Query." />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <StackPanel Grid.Row="3"
                                Margin="0,12,0,0"
                                HorizontalAlignment="Right"
                                Orientation="Horizontal"
                                Visibility="{Binding Review.HasReview, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Button Margin="0,0,8,0"
                                Command="{Binding CancelSynchronizationCommand}"
                                Content="Cancel"
                                Style="{StaticResource SecondaryButtonStyle}" />
                        <Button Command="{Binding ApplySynchronizationCommand}"
                                Content="Import Changes"
                                Style="{StaticResource PrimaryButtonStyle}" />
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Add Entity" Tag="{x:Static viewModels:MainWindowTab.AddEntity}">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Margin="0,0,0,14">
                        <TextBlock FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource Brush.Text.Primary}"
                                   Text="Create a tracked entity" />
                        <TextBlock Margin="0,3,0,0"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   Text="Add a new entity and its initial mandatory dependencies. Unknown names are kept as unresolved warnings."
                                   TextWrapping="Wrap" />
                    </StackPanel>

                    <ScrollViewer Grid.Row="1"
                                  HorizontalScrollBarVisibility="Disabled"
                                  PreviewMouseWheel="OnReviewPreviewMouseWheel"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel MaxWidth="760" HorizontalAlignment="Left">
                            <TextBlock FontWeight="SemiBold" Text="Entity name" />
                            <TextBox Margin="0,6,0,12"
                                     MinWidth="420"
                                     Padding="9,7"
                                     Text="{Binding ManualCreation.EntityName, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock FontWeight="SemiBold" Text="Responsible developer" />
                            <TextBox Margin="0,6,0,18"
                                     MinWidth="420"
                                     Padding="9,7"
                                     Text="{Binding ManualCreation.ResponsibleDeveloper, UpdateSourceTrigger=PropertyChanged}" />

                            <TextBlock FontWeight="SemiBold" Text="Group" />
                            <TextBlock Margin="0,3,0,7"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="Type a new group or choose an existing group below."
                                       TextWrapping="Wrap" />
                            <TextBox MinWidth="420"
                                     Padding="9,7"
                                     Text="{Binding ManualCreation.GroupName, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Margin="0,7,0,0"
                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                       Text="{Binding ManualCreation.GroupSearchMessage}"
                                       TextWrapping="Wrap"
                                       Visibility="{Binding ManualCreation.HasGroupSearchMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <ItemsControl Margin="0,6,0,18"
                                          ItemsSource="{Binding ManualCreation.GroupSuggestions}"
                                          Visibility="{Binding ManualCreation.HasGroupSuggestions, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,5"
                                                Padding="8"
                                                Background="{DynamicResource Brush.Surface.Card}"
                                                BorderBrush="{DynamicResource Brush.Border.Default}"
                                                BorderThickness="1">
                                            <DockPanel>
                                                <Button DockPanel.Dock="Right"
                                                        Padding="12,5"
                                                        Command="{Binding DataContext.ManualCreation.UseGroupSuggestionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Content="Use"
                                                        Style="{StaticResource SecondaryButtonStyle}" />
                                                <TextBlock VerticalAlignment="Center" Text="{Binding}" />
                                            </DockPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock FontWeight="SemiBold" Text="Add dependencies" />
                            <TextBlock Margin="0,3,0,7"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="Search active entities, or deliberately keep an unknown name as unresolved."
                                       TextWrapping="Wrap" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox MinWidth="420"
                                         Padding="9,7"
                                         Text="{Binding ManualCreation.DependencyQuery, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Grid.Column="1"
                                        Margin="8,0,0,0"
                                        Command="{Binding ManualCreation.AddUnresolvedCommand}"
                                        Content="Add as unresolved"
                                        Style="{StaticResource SecondaryButtonStyle}" />
                            </Grid>

                            <TextBlock Margin="0,7,0,0"
                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                       Text="{Binding ManualCreation.SearchMessage}"
                                       TextWrapping="Wrap"
                                       Visibility="{Binding ManualCreation.HasSearchMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />

                            <ItemsControl Margin="0,6,0,0"
                                          ItemsSource="{Binding ManualCreation.Suggestions}"
                                          Visibility="{Binding ManualCreation.HasSuggestions, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,5"
                                                Padding="8"
                                                Background="{DynamicResource Brush.Surface.Card}"
                                                BorderBrush="{DynamicResource Brush.Border.Default}"
                                                BorderThickness="1">
                                            <DockPanel>
                                                <Button DockPanel.Dock="Right"
                                                        Padding="12,5"
                                                        Command="{Binding DataContext.ManualCreation.AddExistingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Content="Add"
                                                        Style="{StaticResource SecondaryButtonStyle}" />
                                                <TextBlock VerticalAlignment="Center" Text="{Binding SourceName}" />
                                            </DockPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock Margin="0,18,0,7"
                                       FontWeight="SemiBold"
                                       Text="Selected dependencies" />
                            <ItemsControl ItemsSource="{Binding ManualCreation.SelectedDependencies}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,6"
                                                Padding="9"
                                                Background="{DynamicResource Brush.Surface.Card}"
                                                BorderBrush="{DynamicResource Brush.Border.Default}"
                                                BorderThickness="1">
                                            <DockPanel>
                                                <Button DockPanel.Dock="Right"
                                                        Padding="12,5"
                                                        Command="{Binding DataContext.ManualCreation.RemoveDependencyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Content="Remove"
                                                        Style="{StaticResource SecondaryButtonStyle}" />
                                                <StackPanel>
                                                    <TextBlock FontWeight="SemiBold" Text="{Binding SourceName}" />
                                                    <TextBlock Margin="0,2,0,0"
                                                               Foreground="{DynamicResource Brush.Text.Secondary}"
                                                               Text="{Binding ResolutionLabel}" />
                                                </StackPanel>
                                            </DockPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Border Margin="0,12,0,0"
                                    MaxHeight="110"
                                    Padding="10"
                                    Background="{DynamicResource Brush.Surface.Subtle}"
                                    BorderBrush="{DynamicResource Brush.Accent}"
                                    BorderThickness="1"
                                    Visibility="{Binding ManualCreation.HasWarnings, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding ManualCreation.Warnings}" />
                                </ScrollViewer>
                            </Border>

                            <Border Margin="0,12,0,0"
                                    Padding="10"
                                    Background="{DynamicResource Brush.Surface.Subtle}"
                                    BorderBrush="{DynamicResource Brush.Accent}"
                                    BorderThickness="1"
                                    Visibility="{Binding ManualCreation.HasArchivedEntityMatch, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <DockPanel>
                                    <Button DockPanel.Dock="Right"
                                            Margin="12,0,0,0"
                                            Command="{Binding ManualCreation.RestoreArchivedCommand}"
                                            Content="Restore existing entity"
                                            Style="{StaticResource SecondaryButtonStyle}" />
                                    <TextBlock VerticalAlignment="Center"
                                               Foreground="{DynamicResource Brush.Text.Primary}"
                                               Text="{Binding ManualCreation.ArchivedEntityMessage}"
                                               TextWrapping="Wrap" />
                                </DockPanel>
                            </Border>

                            <Border Margin="0,12,0,0"
                                    MaxHeight="110"
                                    Padding="10"
                                    Background="{DynamicResource Brush.Surface.Alert}"
                                    BorderBrush="{DynamicResource Brush.Accent}"
                                    BorderThickness="1"
                                    Visibility="{Binding ManualCreation.HasErrors, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding ManualCreation.Errors}" />
                                </ScrollViewer>
                            </Border>

                            <TextBlock Margin="0,12,0,0"
                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                       Text="{Binding ManualCreation.OperationMessage}"
                                       Visibility="{Binding ManualCreation.HasOperationMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </StackPanel>
                    </ScrollViewer>

                    <StackPanel Grid.Row="2"
                                Margin="0,12,0,0"
                                HorizontalAlignment="Right"
                                Orientation="Horizontal">
                        <Button Margin="0,0,8,0"
                                Command="{Binding ManualCreation.CancelCommand}"
                                Content="Cancel"
                                Style="{StaticResource SecondaryButtonStyle}" />
                        <Button Command="{Binding ManualCreation.CreateCommand}"
                                Content="Create Entity"
                                Style="{StaticResource PrimaryButtonStyle}" />
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Reports" Tag="{x:Static viewModels:MainWindowTab.Progress}">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                              PreviewMouseWheel="OnReviewPreviewMouseWheel"
                              VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12">
                        <DockPanel Margin="0,0,0,14">
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <TextBlock Margin="0,0,7,0"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="History" />
                                <ComboBox MinWidth="145"
                                          DisplayMemberPath="DisplayName"
                                          ItemsSource="{Binding Progress.RangeOptions}"
                                          SelectedValue="{Binding Progress.SelectedRange}"
                                          SelectedValuePath="Value" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock FontSize="20"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource Brush.Text.Primary}"
                                           Text="Progress history" />
                                <TextBlock Margin="0,3,0,0"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="Current work status and persisted implementation/readiness trends." />
                            </StackPanel>
                        </DockPanel>

                        <Border Margin="0,0,0,12"
                                Padding="12"
                                Background="{DynamicResource Brush.Surface.Card}"
                                BorderBrush="{DynamicResource Brush.Border.Default}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Visibility="{Binding Progress.IsCustomRange, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Margin="0,0,7,0" VerticalAlignment="Center" Text="From" />
                                    <DatePicker Width="145" SelectedDate="{Binding Progress.CustomFrom}" />
                                    <TextBlock Margin="14,0,7,0" VerticalAlignment="Center" Text="To" />
                                    <DatePicker Width="145" SelectedDate="{Binding Progress.CustomTo}" />
                                    <Button Margin="12,0,0,0"
                                            Command="{Binding Progress.ApplyRangeCommand}"
                                            Content="Apply range"
                                            Style="{StaticResource PrimaryButtonStyle}" />
                                </StackPanel>
                                <TextBlock Margin="0,7,0,0"
                                           Foreground="{DynamicResource Brush.Text.Primary}"
                                           Text="{Binding Progress.RangeValidationMessage}"
                                           Visibility="{Binding Progress.HasRangeValidationError, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            </StackPanel>
                        </Border>

                        <Border Margin="0,0,0,12"
                                MaxHeight="100"
                                Padding="10"
                                Background="{DynamicResource Brush.Surface.Alert}"
                                BorderBrush="{DynamicResource Brush.Accent}"
                                BorderThickness="1"
                                Visibility="{Binding Progress.HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <TextBlock Foreground="{DynamicResource Brush.Text.Primary}"
                                           Text="{Binding Progress.ErrorMessage}"
                                           TextWrapping="Wrap" />
                            </ScrollViewer>
                        </Border>

                        <Border Margin="0,0,0,12"
                                MaxHeight="100"
                                Padding="10"
                                Background="{DynamicResource Brush.Surface.Subtle}"
                                BorderBrush="{DynamicResource Brush.Border.Strong}"
                                BorderThickness="1"
                                Visibility="{Binding Progress.HasExportMessage, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <TextBlock Foreground="{DynamicResource Brush.Text.Primary}"
                                           Text="{Binding Progress.ExportMessage}"
                                           TextWrapping="Wrap" />
                            </ScrollViewer>
                        </Border>

                        <Border Margin="0,0,0,12"
                                Padding="14,12"
                                Background="{DynamicResource Brush.Surface.Card}"
                                BorderBrush="{DynamicResource Brush.Border.Default}"
                                BorderThickness="1"
                                CornerRadius="5"
                                Visibility="{Binding Progress.HasReport, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <DockPanel Margin="0,0,0,10">
                                    <TextBlock DockPanel.Dock="Right"
                                               VerticalAlignment="Center"
                                               Foreground="{DynamicResource Brush.Text.Secondary}"
                                               Text="{Binding Progress.ManagerDateSummary}" />
                                    <TextBlock FontSize="16"
                                               FontWeight="SemiBold"
                                               Text="Manager summary" />
                                </DockPanel>
                                <UniformGrid Columns="5">
                                    <StackPanel>
                                        <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" Text="Active" />
                                        <TextBlock FontSize="22" FontWeight="SemiBold" Text="{Binding Progress.ManagerSummary.ActiveEntityCount}" />
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" Text="Implemented" />
                                        <TextBlock FontSize="22" FontWeight="SemiBold" Text="{Binding Progress.ManagerSummary.ImplementedEntityCount}" />
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" Text="Reconciled" />
                                        <TextBlock FontSize="22" FontWeight="SemiBold" Text="{Binding Progress.ManagerSummary.ReconciledEntityCount}" />
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Foreground="{DynamicResource Brush.Text.Secondary}" Text="Ready" />
                                        <TextBlock FontSize="22" FontWeight="SemiBold" Text="{Binding Progress.ManagerSummary.ReadyEntityCount}" />
                                    </StackPanel>
                                    <Border Margin="6,0,0,0"
                                            Padding="8,4"
                                            Background="{DynamicResource Brush.Status.Blocked}"
                                            CornerRadius="4">
                                        <StackPanel>
                                            <TextBlock Foreground="{DynamicResource Brush.Brand.DarkGreen}" Text="Blocked" />
                                            <TextBlock FontSize="22"
                                                       FontWeight="SemiBold"
                                                       Foreground="{DynamicResource Brush.Brand.DarkGreen}"
                                                       Text="{Binding Progress.ManagerSummary.BlockedEntityCount}" />
                                        </StackPanel>
                                    </Border>
                                </UniformGrid>
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,8"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   Text="No persisted progress history is available in this range."
                                   Visibility="{Binding Progress.ShowNoHistoricalData, Converter={StaticResource BooleanToVisibilityConverter}}" />

                        <UniformGrid Columns="2">
                            <Border Height="290" Margin="0,0,6,12" Padding="12" Background="{DynamicResource Brush.Surface.Card}" BorderBrush="{DynamicResource Brush.Border.Default}" BorderThickness="1" CornerRadius="5">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold" Text="Entities by current work status" />
                                        <Button Grid.Column="1" Margin="8,0,5,0" Padding="10,5"
                                                Command="{Binding Progress.SaveChartCommand}"
                                                CommandParameter="{x:Static reporting:ProgressChartKind.CurrentStatus}"
                                                Content="Save PNG" Style="{StaticResource SecondaryButtonStyle}"
                                                ToolTip="Save a report-ready PNG" />
                                        <Button Grid.Column="2" Padding="10,5"
                                                Command="{Binding Progress.CopyChartCommand}"
                                                CommandParameter="{x:Static reporting:ProgressChartKind.CurrentStatus}"
                                                Content="Copy" Style="{StaticResource SecondaryButtonStyle}"
                                                ToolTip="Copy a report-ready PNG to the clipboard" />
                                    </Grid>
                                    <Border Grid.Row="1" Background="{DynamicResource Brush.Report.Canvas}">
                                        <lvc:PieChart LegendPosition="Right"
                                                      Series="{Binding Progress.CurrentStatusSeries}" />
                                    </Border>
                                </Grid>
                            </Border>
                            <Border Height="290" Margin="6,0,0,12" Padding="12" Background="{DynamicResource Brush.Surface.Card}" BorderBrush="{DynamicResource Brush.Border.Default}" BorderThickness="1" CornerRadius="5">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold" Text="Implemented entities over time" />
                                        <Button Grid.Column="1" Margin="8,0,5,0" Padding="10,5"
                                                Command="{Binding Progress.SaveChartCommand}"
                                                CommandParameter="{x:Static reporting:ProgressChartKind.ImplementedOverTime}"
                                                Content="Save PNG" Style="{StaticResource SecondaryButtonStyle}"
                                                ToolTip="Save a report-ready PNG" />
                                        <Button Grid.Column="2" Padding="10,5"
                                                Command="{Binding Progress.CopyChartCommand}"
                                                CommandParameter="{x:Static reporting:ProgressChartKind.ImplementedOverTime}"
                                                Content="Copy" Style="{StaticResource SecondaryButtonStyle}"
                                                ToolTip="Copy a report-ready PNG to the clipboard" />
                                    </Grid>
                                    <Border Grid.Row="1" Background="{DynamicResource Brush.Report.Canvas}">
                                        <lvc:CartesianChart LegendPosition="Top"
                                                            Series="{Binding Progress.ImplementedSeries}"
                                                            TooltipPosition="Top"
                                                            XAxes="{Binding Progress.ImplementedXAxes}"
                                                            YAxes="{Binding Progress.CountYAxes}" />
                                    </Border>
                                </Grid>
                            </Border>
                            <Border Height="290" Margin="0,0,6,0" Padding="12" Background="{DynamicResource Brush.Surface.Card}" BorderBrush="{DynamicResource Brush.Border.Default}" BorderThickness="1" CornerRadius="5">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold" Text="Ready vs blocked over time" />
                                        <Button Grid.Column="1" Margin="8,0,5,0" Padding="10,5"
                                                Command="{Binding Progress.SaveChartCommand}"
                                                CommandParameter="{x:Static reporting:ProgressChartKind.ReadyAndBlockedOverTime}"
                                                Content="Save PNG" Style="{StaticResource SecondaryButtonStyle}"
                                                ToolTip="Save a report-ready PNG" />
                                        <Button Grid.Column="2" Padding="10,5"
                                                Command="{Binding Progress.CopyChartCommand}"
                                                CommandParameter="{x:Static reporting:ProgressChartKind.ReadyAndBlockedOverTime}"
                                                Content="Copy" Style="{StaticResource SecondaryButtonStyle}"
                                                ToolTip="Copy a report-ready PNG to the clipboard" />
                                    </Grid>
                                    <Border Grid.Row="1" Background="{DynamicResource Brush.Report.Canvas}">
                                        <lvc:CartesianChart LegendPosition="Top"
                                                            Series="{Binding Progress.ReadinessSeries}"
                                                            TooltipPosition="Top"
                                                            XAxes="{Binding Progress.ReadinessXAxes}"
                                                            YAxes="{Binding Progress.CountYAxes}" />
                                    </Border>
                                </Grid>
                            </Border>
                            <Border Height="290" Margin="6,0,0,0" Padding="12" Background="{DynamicResource Brush.Surface.Card}" BorderBrush="{DynamicResource Brush.Border.Default}" BorderThickness="1" CornerRadius="5">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold" Text="Weekly net implemented change" />
                                        <Button Grid.Column="1" Margin="8,0,5,0" Padding="10,5"
                                                Command="{Binding Progress.SaveChartCommand}"
                                                CommandParameter="{x:Static reporting:ProgressChartKind.WeeklyNetImplementedChange}"
                                                Content="Save PNG" Style="{StaticResource SecondaryButtonStyle}"
                                                ToolTip="Save a report-ready PNG" />
                                        <Button Grid.Column="2" Padding="10,5"
                                                Command="{Binding Progress.CopyChartCommand}"
                                                CommandParameter="{x:Static reporting:ProgressChartKind.WeeklyNetImplementedChange}"
                                                Content="Copy" Style="{StaticResource SecondaryButtonStyle}"
                                                ToolTip="Copy a report-ready PNG to the clipboard" />
                                    </Grid>
                                    <Border Grid.Row="1" Background="{DynamicResource Brush.Report.Canvas}">
                                        <lvc:CartesianChart LegendPosition="Top"
                                                            Series="{Binding Progress.WeeklySeries}"
                                                            TooltipPosition="Top"
                                                            XAxes="{Binding Progress.WeeklyXAxes}"
                                                            YAxes="{Binding Progress.SignedYAxes}" />
                                    </Border>
                                </Grid>
                            </Border>
                        </UniformGrid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Help &amp; SQL" Tag="{x:Static viewModels:MainWindowTab.SqlHelp}">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <DockPanel Margin="0,0,0,14">
                        <Button DockPanel.Dock="Right"
                                Command="{Binding Help.BackToImportCommand}"
                                Content="Back to Schema Synchronization"
                                Style="{StaticResource SecondaryButtonStyle}" />
                        <StackPanel>
                            <TextBlock FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                       Text="PostgreSQL schema export" />
                            <TextBlock Margin="0,3,0,0"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="Run this query in PostgreSQL, export its result as a semicolon-delimited CSV, then import that CSV separately." />
                        </StackPanel>
                    </DockPanel>

                    <Border Grid.Row="1"
                            Margin="0,0,0,12"
                            Padding="12"
                            Background="{DynamicResource Brush.Surface.Selected}"
                            BorderBrush="{DynamicResource Brush.Border.Medium}"
                            BorderThickness="1"
                            CornerRadius="4">
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold"
                                       Text="{Binding Help.CsvContractVersion, StringFormat=PostgreSQL · CSV contract v{0}}" />
                            <TextBlock Margin="0,4,0,0"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="The query targets the public schema by default. Change target_schema in the settings section near the top when needed. Keep the six column headers unchanged."
                                       TextWrapping="Wrap" />
                            <TextBlock Margin="0,4,0,0"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="Export as UTF-8 CSV with a semicolon (;) delimiter and include the header row." />
                        </StackPanel>
                    </Border>

                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <DockPanel Margin="0,0,0,8">
                            <Button DockPanel.Dock="Right"
                                    Command="{Binding Help.CopyQueryCommand}"
                                    Content="Copy SQL"
                                    Style="{StaticResource PrimaryButtonStyle}" />
                            <TextBlock VerticalAlignment="Center"
                                       Foreground="{DynamicResource Brush.Text.Primary}"
                                       Text="{Binding Help.CopyMessage}"
                                       Visibility="{Binding Help.HasCopyMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </DockPanel>
                        <TextBox Grid.Row="1"
                                 Padding="12"
                                 AcceptsReturn="True"
                                 AcceptsTab="True"
                                 FontFamily="Consolas"
                                 FontSize="13"
                                 HorizontalScrollBarVisibility="Auto"
                                 IsReadOnly="True"
                                 Text="{Binding Help.Query, Mode=OneWay}"
                                 VerticalScrollBarVisibility="Auto" />
                    </Grid>
                </Grid>
            </TabItem>


        </TabControl>
    </DockPanel>

        <Border x:Name="EntityDetailsPane"
                Width="420"
                MaxWidth="420"
                Margin="18"
                Padding="20"
                Panel.ZIndex="50"
                HorizontalAlignment="Right"
                VerticalAlignment="Stretch"
                AutomationProperties.LiveSetting="Polite"
                Background="{DynamicResource Brush.Surface.Page}"
                BorderBrush="{DynamicResource Brush.Border.Strong}"
                BorderThickness="1"
                CornerRadius="6"
                DataContext="{Binding SelectedEntityDetails}"
                Visibility="{Binding DataContext.IsEntityDetailsOpen, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DockPanel Margin="0,0,0,14">
                    <Button x:Name="CloseEntityDetailsButton"
                            DockPanel.Dock="Right"
                            Width="34"
                            Height="34"
                            Padding="0"
                            AutomationProperties.Name="Close entity details"
                            Command="{Binding DataContext.CloseEntityDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Content="✕"
                            Style="{StaticResource SecondaryButtonStyle}"
                            ToolTip="Close details (Esc)" />
                    <StackPanel>
                        <TextBlock AutomationProperties.HeadingLevel="Level2"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource Brush.Text.Primary}"
                                   Text="{Binding SourceName}"
                                   TextWrapping="Wrap" />
                        <TextBlock Margin="0,3,0,0"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   Text="{Binding Lifecycle}" />
                    </StackPanel>
                </DockPanel>

                <ScrollViewer Grid.Row="1"
                              HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" Text="Status" />
                        <WrapPanel Margin="0,7,0,16">
                            <ContentControl Margin="0,0,8,6"
                                            AutomationProperties.Name="{Binding WorkStatus, StringFormat=Work status: {0}}"
                                            Content="{Binding WorkflowStateValue}"
                                            ContentTemplate="{StaticResource WorkStatusBadgeTemplate}" />
                            <ContentControl Margin="0,0,0,6"
                                            AutomationProperties.Name="{Binding DevelopmentStatus, StringFormat=Development status: {0}}"
                                            Content="{Binding DevelopmentStatusValue}"
                                            ContentTemplate="{StaticResource DevelopmentStatusBadgeTemplate}" />
                        </WrapPanel>

                        <Grid Margin="0,0,0,16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,12,0">
                                <TextBlock FontWeight="SemiBold" Text="Requested priority" />
                                <TextBlock Margin="0,3,0,0" Text="{Binding RequestedPriority}" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock FontWeight="SemiBold" Text="Effective priority" />
                                <TextBlock Margin="0,3,0,0" Text="{Binding EffectivePriority}" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Margin="0,12,12,0">
                                <TextBlock FontWeight="SemiBold" Text="Rank" />
                                <TextBlock Margin="0,3,0,0" Text="{Binding Rank}" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,12,0,0">
                                <TextBlock FontWeight="SemiBold" Text="Provenance" />
                                <TextBlock Margin="0,3,0,0" Text="{Binding Provenance}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,12,0">
                                <TextBlock FontWeight="SemiBold" Text="Responsible developer" />
                                <TextBlock Margin="0,3,0,0" Text="{Binding ResponsibleDeveloper}" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock FontWeight="SemiBold" Text="Group" />
                                <TextBlock Margin="0,3,0,0" Text="{Binding GroupName}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Grid>

                        <TextBlock FontWeight="SemiBold" Text="Notes" />
                        <TextBlock Margin="0,4,0,18"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   Text="{Binding Notes}"
                                   TextWrapping="Wrap" />

                        <Border Margin="0,0,0,16"
                                Padding="10"
                                Background="{DynamicResource Brush.Surface.Alert}"
                                BorderBrush="{DynamicResource Brush.Accent}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Visibility="{Binding HasGraphIssue, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock FontWeight="SemiBold" Text="{Binding GraphIssueTitle}" />
                                <TextBlock Margin="0,4,0,0"
                                           Text="{Binding GraphIssueDescription}"
                                           TextWrapping="Wrap" />
                                <TextBlock Margin="0,5,0,0"
                                           FontWeight="SemiBold"
                                           Text="{Binding GraphIssueNames}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <DockPanel Margin="0,0,0,6">
                            <TextBlock DockPanel.Dock="Right"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="{Binding DependencyCount, StringFormat=Count: {0}}" />
                            <TextBlock FontWeight="SemiBold" Text="{Binding DependencySectionTitle}" />
                        </DockPanel>
                        <ItemsControl Margin="0,0,0,16"
                                      ItemsSource="{Binding Dependencies}"
                                      Visibility="{Binding HasDependencies, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Margin="0,2" Text="{Binding Name, StringFormat=• {0}}" TextWrapping="Wrap" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Margin="0,0,0,16"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   Text="None">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasDependencies}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <TextBlock AutomationProperties.HelpText="Unresolved or not-yet-implemented direct dependencies"
                                   FontWeight="SemiBold"
                                   Text="Blockers" />
                        <ItemsControl Margin="0,6,0,16"
                                      ItemsSource="{Binding Blockers}"
                                      Visibility="{Binding HasBlockers, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,7">
                                        <TextBlock FontWeight="SemiBold" Text="{Binding Name}" TextWrapping="Wrap" />
                                        <TextBlock Margin="0,2,0,0"
                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   Text="{Binding Description}"
                                                   TextWrapping="Wrap" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Margin="0,6,0,16"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   Text="{Binding BlockersEmptyMessage}"
                                   TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasBlockers}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <TextBlock FontWeight="SemiBold" Text="Audit timestamps" />
                        <Grid Margin="0,6,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Created" />
                            <TextBlock Grid.Column="1" Text="{Binding CreatedAt}" TextWrapping="Wrap" />
                            <TextBlock Grid.Row="1" Margin="0,5,0,0" Text="Schema/lifecycle updated" TextWrapping="Wrap" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,5,0,0" Text="{Binding SchemaUpdatedAt}" TextWrapping="Wrap" />
                            <TextBlock Grid.Row="2" Margin="0,5,0,0" Text="Status/notes updated" TextWrapping="Wrap" />
                            <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,5,0,0" Text="{Binding ProgressUpdatedAt}" TextWrapping="Wrap" />
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <Grid x:Name="EditorOverlay"
              Panel.ZIndex="100"
              Background="{DynamicResource Brush.Overlay}"
              FocusManager.IsFocusScope="True"
              KeyboardNavigation.ControlTabNavigation="Cycle"
              KeyboardNavigation.DirectionalNavigation="Cycle"
              KeyboardNavigation.TabNavigation="Cycle"
              Visibility="{Binding Editor.IsOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border x:Name="EditorSurface"
                    MinWidth="700"
                    MaxWidth="900"
                    MaxHeight="660"
                    Margin="30"
                    Padding="22"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="{DynamicResource Brush.Surface.Page}"
                    BorderBrush="{DynamicResource Brush.Border.Medium}"
                    BorderThickness="1"
                    CornerRadius="7"
                    Focusable="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Margin="0,0,0,14">
                        <TextBlock FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource Brush.Text.Primary}"
                                   Text="{Binding Editor.ContextTitle}" />
                        <TextBlock Margin="0,3,0,0"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   Text="{Binding Editor.ContextDescription}"
                                   TextWrapping="Wrap" />
                    </StackPanel>

                    <ScrollViewer Grid.Row="1"
                                  HorizontalScrollBarVisibility="Disabled"
                                  PreviewMouseWheel="OnReviewPreviewMouseWheel"
                                  VerticalScrollBarVisibility="Auto">
                        <StackPanel MinWidth="650" MaxWidth="820" HorizontalAlignment="Left">
                            <TextBlock FontSize="17"
                                       FontWeight="SemiBold"
                                       Text="{Binding Editor.SelectedEntityName}" />
                            <TextBlock Margin="0,3,0,16"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="{Binding Editor.EntityDetails}" />
                            <StackPanel Visibility="{Binding Editor.HasSelectedEntity, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Margin="0,0,12,0">
                                        <TextBlock FontWeight="SemiBold" Text="Development status" />
                                        <ComboBox x:Name="EditorStatusComboBox"
                                                  Margin="0,5,0,0"
                                                  DisplayMemberPath="DisplayName"
                                                  IsEnabled="{Binding Editor.CanEditProgress}"
                                                  ItemsSource="{Binding Editor.StatusOptions}"
                                                  SelectedValue="{Binding Editor.SelectedStatus}"
                                                  SelectedValuePath="Value" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock FontWeight="SemiBold" Text="Notes" />
                                        <TextBox MinHeight="64"
                                                 Margin="0,5,0,0"
                                                 Padding="8"
                                                 AcceptsReturn="True"
                                                 IsEnabled="{Binding Editor.CanEditProgress}"
                                                 Text="{Binding Editor.EditedNotes, UpdateSourceTrigger=PropertyChanged}"
                                                 TextWrapping="Wrap" />
                                    </StackPanel>
                                </Grid>

                                <StackPanel Margin="0,0,0,12">
                                    <TextBlock FontWeight="SemiBold" Text="Responsible developer" />
                                    <TextBox Margin="0,5,0,0"
                                             Padding="8,6"
                                             IsEnabled="{Binding Editor.CanEditProgress}"
                                             Text="{Binding Editor.EditedResponsibleDeveloper, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>

                                <StackPanel Margin="0,0,0,12">
                                    <TextBlock FontWeight="SemiBold" Text="Group" />
                                    <TextBox Margin="0,5,0,0"
                                             Padding="8,6"
                                             IsEnabled="{Binding Editor.CanEditProgress}"
                                             Text="{Binding Editor.EditedGroupName, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Margin="0,7,0,0"
                                               Foreground="{DynamicResource Brush.Text.Primary}"
                                               Text="{Binding Editor.GroupSearchMessage}"
                                               TextWrapping="Wrap"
                                               Visibility="{Binding Editor.HasGroupSearchMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    <ItemsControl Margin="0,6,0,0"
                                                  ItemsSource="{Binding Editor.GroupSuggestions}"
                                                  Visibility="{Binding Editor.HasGroupSuggestions, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="0,0,0,5"
                                                        Padding="8"
                                                        Background="{DynamicResource Brush.Surface.Page}"
                                                        BorderBrush="{DynamicResource Brush.Border.Default}"
                                                        BorderThickness="1">
                                                    <DockPanel>
                                                        <Button DockPanel.Dock="Right"
                                                                Padding="12,5"
                                                                Command="{Binding DataContext.Editor.UseGroupSuggestionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Content="Use"
                                                                Style="{StaticResource SecondaryButtonStyle}" />
                                                        <TextBlock VerticalAlignment="Center" Text="{Binding}" />
                                                    </DockPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Margin="0,0,12,0">
                                        <TextBlock FontWeight="SemiBold" Text="Requested priority" />
                                        <ComboBox Margin="0,5,0,0"
                                                  DisplayMemberPath="DisplayName"
                                                  IsEnabled="{Binding Editor.CanEditPriority}"
                                                  ItemsSource="{Binding Editor.PriorityOptions}"
                                                  SelectedValue="{Binding Editor.SelectedRequestedPriority}"
                                                  SelectedValuePath="Value" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock FontWeight="SemiBold" Text="Effective priority" />
                                        <TextBlock Margin="0,8,0,0"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   Text="{Binding Editor.EffectivePriority}" />
                                        <TextBlock Margin="0,2,0,0"
                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   Text="Inherited from this entity or an active dependent target."
                                                   TextWrapping="Wrap" />
                                    </StackPanel>
                                </Grid>

                                <Border Margin="0,0,0,14"
                                        Padding="10"
                                        Background="{DynamicResource Brush.Surface.Subtle}"
                                        BorderBrush="{DynamicResource Brush.Border.Medium}"
                                        BorderThickness="1"
                                        Visibility="{Binding Editor.HasPendingPriorityChange, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <StackPanel>
                                        <TextBlock FontWeight="SemiBold" Text="Priority impact preview" />
                                        <TextBlock Margin="0,2,0,8"
                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   Text="This shows the target and resolved prerequisites affected when you save."
                                                   TextWrapping="Wrap" />
                                        <ItemsControl ItemsSource="{Binding Editor.PriorityPreviewRows}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,0,0,4">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="110" />
                                                            <ColumnDefinition Width="100" />
                                                            <ColumnDefinition Width="100" />
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock FontWeight="SemiBold" Text="{Binding SourceName}" />
                                                        <TextBlock Grid.Column="1" Text="{Binding Role}" />
                                                        <TextBlock Grid.Column="2" Text="{Binding RequestedPriority, StringFormat=Requested: {0}}" />
                                                        <TextBlock Grid.Column="3" Text="{Binding EffectivePriority, StringFormat=Effective: {0}}" />
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        <Border Margin="0,7,0,0"
                                                Padding="8"
                                                Background="{DynamicResource Brush.Surface.Page}"
                                                BorderBrush="{DynamicResource Brush.Accent}"
                                                BorderThickness="1"
                                                Visibility="{Binding Editor.HasPriorityUnresolvedDependencies, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <StackPanel>
                                                <TextBlock FontWeight="SemiBold"
                                                           Text="Unresolved dependencies cannot receive priority:" />
                                                <ItemsControl Margin="0,3,0,0"
                                                              ItemsSource="{Binding Editor.PriorityUnresolvedDependencyNames}" />
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </Border>

                                <StackPanel Visibility="{Binding Editor.ShowDependencyEditor, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock FontWeight="SemiBold" Text="Add a manual dependency" />
                                <TextBlock Margin="0,3,0,7"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"
                                           Text="Search active entities, or deliberately retain an unknown name as unresolved."
                                           TextWrapping="Wrap" />
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox MinWidth="420"
                                             Padding="9,7"
                                             Text="{Binding Editor.DependencyQuery, UpdateSourceTrigger=PropertyChanged}" />
                                    <Button Grid.Column="1"
                                            Margin="8,0,0,0"
                                            Command="{Binding Editor.AddUnresolvedCommand}"
                                            Content="Add as unresolved"
                                            Style="{StaticResource SecondaryButtonStyle}" />
                                </Grid>

                                <TextBlock Margin="0,7,0,0"
                                           Foreground="{DynamicResource Brush.Text.Primary}"
                                           Text="{Binding Editor.SearchMessage}"
                                           TextWrapping="Wrap"
                                           Visibility="{Binding Editor.HasSearchMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                <ItemsControl Margin="0,6,0,0"
                                              ItemsSource="{Binding Editor.Suggestions}"
                                              Visibility="{Binding Editor.HasSuggestions, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="0,0,0,5" Padding="8" Background="{DynamicResource Brush.Surface.Page}" BorderBrush="{DynamicResource Brush.Border.Default}" BorderThickness="1">
                                                <DockPanel>
                                                    <Button DockPanel.Dock="Right"
                                                            Command="{Binding DataContext.Editor.AddExistingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"
                                                            Content="Add"
                                                            Style="{StaticResource SecondaryButtonStyle}" />
                                                    <TextBlock VerticalAlignment="Center" Text="{Binding SourceName}" />
                                                </DockPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                </StackPanel>

                                <TextBlock Margin="0,18,0,7" FontWeight="SemiBold" Text="Dependencies and overrides" />
                                <ItemsControl ItemsSource="{Binding Editor.Dependencies}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="0,0,0,6" Padding="9" Background="{DynamicResource Brush.Surface.Page}" BorderBrush="{DynamicResource Brush.Border.Default}" BorderThickness="1">
                                                <DockPanel>
                                                    <StackPanel DockPanel.Dock="Right"
                                                                Orientation="Horizontal"
                                                                Visibility="{Binding DataContext.Editor.ShowDependencyEditor, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <Button Margin="6,0,0,0"
                                                                Command="{Binding DataContext.Editor.SuppressCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Content="Suppress imported"
                                                                Style="{StaticResource SecondaryButtonStyle}"
                                                                Visibility="{Binding CanSuppress, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                        <Button Margin="6,0,0,0"
                                                                Command="{Binding DataContext.Editor.RemoveManualCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Content="Remove manual addition"
                                                                Style="{StaticResource SecondaryButtonStyle}"
                                                                Visibility="{Binding CanRemoveManual, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                        <Button Margin="6,0,0,0"
                                                                Command="{Binding DataContext.Editor.RestoreCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Content="Restore imported"
                                                                Style="{StaticResource SecondaryButtonStyle}"
                                                                Visibility="{Binding CanRestore, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                    </StackPanel>
                                                    <StackPanel>
                                                        <TextBlock FontWeight="SemiBold" Text="{Binding SourceName}" />
                                                        <TextBlock Margin="0,2,0,0" Foreground="{DynamicResource Brush.Text.Secondary}" Text="{Binding Origin}" />
                                                        <TextBlock Margin="0,2,0,0" Foreground="{DynamicResource Brush.Text.Secondary}" Text="{Binding Resolution}" />
                                                    </StackPanel>
                                                </DockPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Border Margin="0,12,0,0"
                                        MaxHeight="110"
                                        Padding="10"
                                        Background="{DynamicResource Brush.Surface.Subtle}"
                                        BorderBrush="{DynamicResource Brush.Accent}"
                                        BorderThickness="1"
                                        Visibility="{Binding Editor.HasWarnings, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding Editor.Warnings}" />
                                    </ScrollViewer>
                                </Border>

                                <Border Margin="0,12,0,0"
                                        MaxHeight="110"
                                        Padding="10"
                                        Background="{DynamicResource Brush.Surface.Alert}"
                                        BorderBrush="{DynamicResource Brush.Accent}"
                                        BorderThickness="1"
                                        Visibility="{Binding Editor.HasErrors, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding Editor.Errors}" />
                                    </ScrollViewer>
                                </Border>

                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>

                    <DockPanel Grid.Row="2" Margin="0,12,0,0">
                        <Button DockPanel.Dock="Left"
                                Command="{Binding Editor.RequestArchiveCommand}"
                                Content="Archive entity"
                                Style="{StaticResource DangerButtonStyle}"
                                Visibility="{Binding Editor.CanArchive, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <Button DockPanel.Dock="Left"
                                Margin="8,0,0,0"
                                Command="{Binding Editor.RestoreEntityCommand}"
                                Content="Restore entity"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Visibility="{Binding Editor.CanRestoreEntity, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                            <Button Margin="0,0,8,0"
                                    Command="{Binding Editor.CancelCommand}"
                                    Content="Cancel"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                            <Button Command="{Binding Editor.SaveCommand}"
                                    Content="{Binding Editor.SaveLabel}"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Visibility="{Binding Editor.ShowSave, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </StackPanel>
                    </DockPanel>

                    <Grid Grid.RowSpan="3"
                          Panel.ZIndex="10"
                          Background="{DynamicResource Brush.Overlay}"
                          FocusManager.IsFocusScope="True"
                          FocusManager.FocusedElement="{Binding ElementName=CancelArchiveButton}"
                          Visibility="{Binding Editor.IsArchiveConfirmationOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Border x:Name="ArchiveConfirmationSurface"
                                Width="560"
                                MaxWidth="560"
                                Padding="22"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Background="{DynamicResource Brush.Surface.Page}"
                                BorderBrush="{DynamicResource Brush.Accent}"
                                BorderThickness="1"
                                CornerRadius="6">
                            <StackPanel>
                                <TextBlock FontSize="20"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource Brush.Text.Primary}"
                                           Text="Archive entity?" />
                                <TextBlock Margin="0,10,0,0"
                                           Foreground="{DynamicResource Brush.Text.Primary}"
                                           Text="{Binding Editor.ArchiveConfirmationMessage}"
                                           TextWrapping="Wrap" />
                                <TextBlock Margin="0,10,0,0"
                                           Foreground="{DynamicResource Brush.Text.Primary}"
                                           Text="{Binding Editor.ArchiveErrorMessage}"
                                           TextWrapping="Wrap"
                                           Visibility="{Binding Editor.HasArchiveError, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <StackPanel Margin="0,20,0,0"
                                            HorizontalAlignment="Right"
                                            Orientation="Horizontal">
                                    <Button x:Name="CancelArchiveButton"
                                            Margin="0,0,8,0"
                                            Command="{Binding Editor.CancelArchiveCommand}"
                                            Content="Cancel — keep entity"
                                            IsDefault="True"
                                            Style="{StaticResource PrimaryButtonStyle}" />
                                    <Button Command="{Binding Editor.ConfirmArchiveCommand}"
                                            Content="Archive entity"
                                            Style="{StaticResource DangerButtonStyle}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
